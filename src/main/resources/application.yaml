spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doc_review?useSSL=false&serverTimezone=Asia/Taipei
    username: root
    password: root1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

jwt:
  secret: my-super-secret-key-that-is-long-enough-32chars
  expiration: 86400000

file:
  upload-dir: uploads